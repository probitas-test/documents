{
  "tasks": {
    "dev": "deno run --allow-net --watch main.ts",
    "start": "deno run --allow-net main.ts",
    "check": "deno check ./**/*.ts",
    "verify": "deno fmt && deno lint && deno task check"
  },
  "imports": {
    "hono": "jsr:@hono/hono@^4"
  }
}
